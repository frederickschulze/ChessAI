# This file describes the chessboard class
import numpy as np

# Storing the state of the chess board with square mapping defined as:
# a1 = 0, b1 = 1, c1 = 2... h1 = 7, a2 = 8, b2 = 9, ... h8 = 63
board = [0] * 64

def get_square_name(int i):
